import 'package:flutter/material.dart';

class CompanyPerks extends StatelessWidget {
  const CompanyPerks({super.key, required this.perks});
  final List<String> perks;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
